#!/bin/sh
# shellcheck disable=SC1117,SC1083,SC1073,SC1056,SC1072
# dmenu not load setxkbmap in xinitrc

super + Escape
	pkill -USR1 -x sxhkd

super + f
    $HOME/scripts/bin/launch_brave
    # DRI_PRIME=1 launch_brave
    #firefox

# shift + Print
#   $HOME/scripts/video/record.sh

Print
  flameshot gui

ctrl + shift + 1
  setxkbmap us

ctrl + shift + 2
  setxkbmap ara

super + shift + p
    passmenu

super + w
    iwgtk
    # wpa_gui 

super + q
  bash /tmp/run

super + b
  binggpt

super + shift + n
  neovide

# use xdotool to grep url
super + shift + y
  /home/mhamdi/scripts/bin/yt use_xdotool >/tmp/log.yt.log 2>&1

# use url that already yanked
super + y
  /home/mhamdi/scripts/bin/yt >/tmp/log.yt.log 2>&1

super + s
  bash /home/mhamdi/scripts/bin/super >/tmp/log.super.log 2>&1


#super + shift + q
#  bash /root/scripts/dmenu/add-word.sh

# super + n
#   nv

# super + m
#     mdbook serve /root/book/ayoubelmhamdi.github.io -p 8002

# super + shift + s
#     surfer http://localhost:8002

super + shift + p
  bash ~/scripts/my/dmenu/dmenu_command.sh

super + o
mpv --loop "$(xclip -o -sel clip)"

# #connect
# super + shift + c
#     kdeconnect-cli -d ca4847d95527874b --pair

# super + k
#     kdeconnect-cli -d ca4847d95527874b --share-text $(xclip -o -sel clip)

# super + c
#   wpa_cli reassociate 1


XF86Audio{Raise,Lower}Volume
    amixer sset Master {10+,10-} unmute

XF86AudioMute
    amixer sset 'Master' 70%

# alt + c
#   xdotool key --clearmodifiers ctrl+shift+c &&   sleep 0.1 && xclip -o -sel -clip > /tmp/sxhkd_paste && echo "---alt+c---"
#
# alt + v
#   xdotool key --clearmodifiers ctrl+shift+v && echo "---alt+v---"
